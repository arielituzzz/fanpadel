import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: "20%",
    title: {
      alignItems: "center",
      text: {
        fontSize: 30,
        fontWeight: "bold",
        color: "white",
      },
    },
  },
});

export default styles;
